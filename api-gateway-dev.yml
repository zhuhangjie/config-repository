zuul:
  routes:
    first:
      path: /zhuhangjie/**
      serviceId: product
      sensitiveHeaders:
    second:
      path: /userinfo/**
      serviceId: user
      sensitiveHeaders:
  ignored-patterns:
management:
  security:
    enabled: false
